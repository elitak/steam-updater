# ──────────────────────────────────────────────────────────────────────────────
#  steam-updater — settings.yml
#
#  Structure:
#    library_root  (optional) — where to install games.
#                               Defaults to C:\SteamLibrary
#
#    accounts:
#      <steam_username>:        ← the key IS the Steam login name
#        password: "<steam_password>"
#        appIDs:                ← optional: explicit numeric App IDs
#          - <appid>
#          - <appid>
#        appREs:                ← optional: regex patterns matched against
#          - '<regex>'            Steam app titles (Steam API lookup)
#
#  ⚠  Steam Guard / Mobile Authenticator must be DISABLED on every
#     account listed here. SteamCMD cannot handle 2-FA interactively.
# ──────────────────────────────────────────────────────────────────────────────

library_root: "C:\SteamLibrary"

accounts:
  my_steam_user:
    password: "my_steam_password"
    appIDs:
      - 730      # Counter-Strike 2
      - 570      # Dota 2
      - 440      # Team Fortress 2
    appREs:
      - '^Portal$'             # exact title match

  another_user:
    password: "another_password"
    appIDs:
      - 1091500  # Cyberpunk 2077
      - 1172470  # Apex Legends
